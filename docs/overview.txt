Perfect 👍 let’s start **cleanly and correctly** with **Step 1: Database + SQL creation**.
I’ll keep this **simple, Java-friendly, examiner-safe**, and **easy to extend later**.

---

# 🗄️ STEP 1: DATABASE DESIGN & SETUP

### (SQLite – Zero Cost, Easy with Java)

---

## ✅ Why SQLite (Final Justification)

* No server needed
* One `.db` file
* Works perfectly with Java (JDBC)
* Ideal for **crowdsourced complaints + reviews**
* Accepted in academic projects

👉 File name: `smart_route.db`

---

# 📌 ENTITIES WE NEED (Based on Your Requirements)

From what you said, we must store:

1. Roads
2. Road Metrics (dynamic)
3. Complaints (user-generated)
4. Reviews (ratings + feedback)
5. Users (basic, optional but good)

---

# 🧱 DATABASE SCHEMA (FINAL & NORMALIZED)

---

## 1️⃣ USERS TABLE

(For complaint & review ownership)

```sql
CREATE TABLE users (
    user_id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    email TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

💡 You can keep login **optional** (name only is enough for now).

---

## 2️⃣ NODES TABLE (Map Points)

```sql
CREATE TABLE nodes (
    node_id TEXT PRIMARY KEY,
    latitude REAL NOT NULL,
    longitude REAL NOT NULL
);
```

Example:

* A, B, C, D (or real coordinates)

---

## 3️⃣ ROADS TABLE (Edges)

```sql
CREATE TABLE roads (
    road_id INTEGER PRIMARY KEY AUTOINCREMENT,
    from_node TEXT NOT NULL,
    to_node TEXT NOT NULL,
    distance REAL NOT NULL,
    FOREIGN KEY (from_node) REFERENCES nodes(node_id),
    FOREIGN KEY (to_node) REFERENCES nodes(node_id)
);
```

---

## 4️⃣ ROAD_METRICS TABLE (CORE OF PROJECT)

This table **changes dynamically** based on complaints & reviews.

```sql
CREATE TABLE road_metrics (
    road_id INTEGER PRIMARY KEY,
    surface_condition INTEGER CHECK(surface_condition BETWEEN 1 AND 5),
    traffic_density INTEGER CHECK(traffic_density BETWEEN 1 AND 5),
    safety_score INTEGER CHECK(safety_score BETWEEN 1 AND 5),
    weather_impact INTEGER CHECK(weather_impact BETWEEN 1 AND 5),
    obstacle_count INTEGER DEFAULT 0,
    last_updated DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (road_id) REFERENCES roads(road_id)
);
```

📊 Scale:

* 1 = worst
* 5 = best

---

## 5️⃣ COMPLAINTS TABLE (Crowdsourcing)

```sql
CREATE TABLE complaints (
    complaint_id INTEGER PRIMARY KEY AUTOINCREMENT,
    road_id INTEGER NOT NULL,
    user_id INTEGER,
    complaint_type TEXT NOT NULL,
    description TEXT,
    status TEXT DEFAULT 'OPEN',
    reported_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (road_id) REFERENCES roads(road_id),
    FOREIGN KEY (user_id) REFERENCES users(user_id)
);
```

### Complaint types (examples):

* pothole
* under_construction
* muddy
* flooded
* accident
* blocked

---

## 6️⃣ REVIEWS TABLE (Ratings System)

```sql
CREATE TABLE reviews (
    review_id INTEGER PRIMARY KEY AUTOINCREMENT,
    road_id INTEGER NOT NULL,
    user_id INTEGER,
    rating INTEGER CHECK(rating BETWEEN 1 AND 5),
    comment TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (road_id) REFERENCES roads(road_id),
    FOREIGN KEY (user_id) REFERENCES users(user_id)
);
```

---

# 🔁 HOW DATA AFFECTS METRICS (LOGIC)

### When a complaint is added:

* `obstacle_count += 1`
* `safety_score -= 1` (min 1)
* `surface_condition -= 1` (if pothole/muddy)

### When a review is added:

* Average rating affects:

  * safety_score
  * surface_condition

🔥 This directly feeds into **Dijkstra weights**

---

# ⚙️ WEIGHT FORMULA (Stored in Code, Not DB)

```text
weight =
distance
+ obstacle_count * 2
+ (5 - surface_condition) * 2
+ traffic_density * 1.5
+ (5 - safety_score)
+ weather_impact
```

This is **exactly aligned** with your abstract.

---

# 🧪 SAMPLE INSERT DATA (FOR TESTING)

### Insert Nodes

```sql
INSERT INTO nodes VALUES ('A', 23.2599, 77.4126);
INSERT INTO nodes VALUES ('B', 23.2605, 77.4200);
INSERT INTO nodes VALUES ('C', 23.2550, 77.4250);
```

---

### Insert Roads

```sql
INSERT INTO roads (from_node, to_node, distance)
VALUES ('A', 'B', 3.0),
       ('B', 'C', 5.0),
       ('A', 'C', 9.0);
```

---

### Insert Initial Metrics

```sql
INSERT INTO road_metrics
VALUES (1, 4, 3, 4, 2, 0, CURRENT_TIMESTAMP);
```

---

# 🧠 EXAMINER-READY ANSWER

**Q: Why separate road_metrics table?**

> “To allow dynamic updates based on user complaints and reviews without modifying static road structure.”

✔ Very strong answer.

---

# 📌 WHAT YOU HAVE NOW

✅ Complete DB design
✅ Crowd complaint support
✅ Review system
✅ Ready for Dijkstra
✅ Future-proof

---

## 🔥 NEXT STEPS

Nice — this is the **right order** 👍
I’ll give you **all 4 things in one clean, connected explanation**, so everything fits together logically and you don’t feel lost.

I’ll keep it **easy Java**, **exam-friendly**, and **actually buildable**.

---

# 1️⃣ JAVA BACKEND SKELETON (LIGHTWEIGHT, EASY)

We’ll use **plain Java + Spark Java (micro framework)**
Why Spark?

* Much simpler than Spring Boot
* Still a real backend
* Perfect for college projects

---

## 📁 Backend Folder Structure

```
backend/
│
├── MainServer.java
├── DBConnection.java
│
├── models/
│   ├── Node.java
│   ├── Road.java
│   ├── RoadMetrics.java
│   ├── Complaint.java
│   └── Review.java
│
├── services/
│   ├── DijkstraService.java
│   ├── ComplaintService.java
│   └── ReviewService.java
│
└── routes/
    ├── PathRoutes.java
    ├── ComplaintRoutes.java
    └── ReviewRoutes.java
```

---

## 🔹 MainServer.java (Entry Point)

```java
import static spark.Spark.*;

public class MainServer {
    public static void main(String[] args) {

        port(8080);

        // Enable CORS
        options("/*", (req, res) -> {
            res.header("Access-Control-Allow-Origin", "*");
            res.header("Access-Control-Allow-Methods", "GET,POST,OPTIONS");
            return "OK";
        });

        before((req, res) -> res.header("Access-Control-Allow-Origin", "*"));

        // Register routes
        PathRoutes.register();
        ComplaintRoutes.register();
        ReviewRoutes.register();

        System.out.println("Server started at http://localhost:8080");
    }
}
```

---

## 🔹 DBConnection.java (SQLite)

```java
import java.sql.Connection;
import java.sql.DriverManager;

public class DBConnection {
    private static final String URL = "jdbc:sqlite:database/smart_route.db";

    public static Connection getConnection() {
        try {
            return DriverManager.getConnection(URL);
        } catch (Exception e) {
            e.printStackTrace();
            return null;
        }
    }
}
```

---

# 2️⃣ ENHANCED DIJKSTRA (JAVA – CORE LOGIC)

This is the **heart of your project** 💙

---

## 🔹 Weight Formula (From Your Metrics)

```
weight =
distance
+ obstacle_count * 2
+ (5 - surface_condition) * 2
+ traffic_density * 1.5
+ (5 - safety_score)
+ weather_impact
```

---

## 🔹 DijkstraService.java (Simplified & Clear)

```java
import java.util.*;

public class DijkstraService {

    public static class Edge {
        String to;
        double weight;

        Edge(String to, double weight) {
            this.to = to;
            this.weight = weight;
        }
    }

    public static Map<String, Object> findShortestPath(
            Map<String, List<Edge>> graph,
            String source,
            String destination) {

        Map<String, Double> dist = new HashMap<>();
        Map<String, String> prev = new HashMap<>();

        PriorityQueue<String> pq =
                new PriorityQueue<>(Comparator.comparingDouble(dist::get));

        for (String node : graph.keySet()) {
            dist.put(node, Double.MAX_VALUE);
        }

        dist.put(source, 0.0);
        pq.add(source);

        while (!pq.isEmpty()) {
            String current = pq.poll();

            if (current.equals(destination)) break;

            for (Edge edge : graph.get(current)) {
                double newDist = dist.get(current) + edge.weight;
                if (newDist < dist.get(edge.to)) {
                    dist.put(edge.to, newDist);
                    prev.put(edge.to, current);
                    pq.add(edge.to);
                }
            }
        }

        // Build path
        List<String> path = new ArrayList<>();
        String step = destination;
        while (step != null) {
            path.add(step);
            step = prev.get(step);
        }
        Collections.reverse(path);

        Map<String, Object> result = new HashMap<>();
        result.put("path", path);
        result.put("distance", dist.get(destination));

        return result;
    }
}
```

👉 You’ll build the graph **from DB data** before calling this.

---

# 3️⃣ COMPLAINT & REVIEW API DESIGN (VERY IMPORTANT)

These APIs connect **users → database → routing logic**.

---

## 🔹 Complaint API

### POST `/complaints`

**Purpose:** User reports road issue

**Request JSON**

```json
{
  "road_id": 2,
  "user_name": "Charu",
  "complaint_type": "pothole",
  "description": "Large potholes near junction"
}
```

**Backend Logic**

1. Insert into `complaints`
2. Update `road_metrics`

   * obstacle_count +1
   * surface_condition −1
   * safety_score −1

---

### GET `/complaints/:roadId`

Returns all complaints for a road.

---

## 🔹 Review API

### POST `/reviews`

```json
{
  "road_id": 2,
  "rating": 4,
  "comment": "Road is okay now"
}
```

**Backend Logic**

1. Insert review
2. Recalculate average rating
3. Update:

   * safety_score
   * surface_condition

---

## 🔹 Path API

### POST `/path`

```json
{
  "source": "A",
  "destination": "C"
}
```

**Response**

```json
{
  "path": ["A", "B", "C"],
  "distance": 12.4,
  "risk": "LOW",
  "alerts": ["Pothole on B-C"]
}
```

🔥 This is examiner GOLD.

---

# 4️⃣ FRONTEND UI WIREFRAME (TEXTUAL – FOR IMPLEMENTATION & VIVA)

---

## 🖥️ MAIN SCREEN LAYOUT

```
+--------------------------------------------------+
| Smart Route Analyzer                              |
+--------------------------------------------------+
| [ Map Area - OpenStreetMap ]                     |
|                                                  |
|  ● Nodes                                         |
|  ─ Roads (color-coded)                           |
|                                                  |
+----------------------+---------------------------+
| Control Panel        | Route Summary              |
|----------------------|---------------------------|
| Source: [Dropdown]  | Distance: 12.4 km          |
| Destination:[Drop]  | Time: 18 min               |
|                      | Road Quality: High        |
| [ Find Path ]       | Alerts: ⚠ pothole         |
+----------------------+---------------------------+
```

---

## 🚧 COMPLAINT PANEL (Popup / Side Panel)

```
Report Road Issue
-----------------
Road: A → B
Issue Type:
( ) Pothole
( ) Construction
( ) Muddy
( ) Flooded

Description:
[__________________]

[ Submit Complaint ]
```

---

## ⭐ REVIEW PANEL

```
Rate This Road
--------------
⭐⭐⭐⭐☆ (4/5)

Comment:
"Smooth drive but traffic is heavy"

[ Submit Review ]
```

---

# 🧠 HOW EVERYTHING CONNECTS (ONE LINE)

> User complaints and reviews dynamically update road metrics in the database, which directly affect the edge weights used by the enhanced Dijkstra algorithm, resulting in safer and more realistic route selection.

🔥 You can literally say this in viva.

---

# ✅ WHAT YOU HAVE NOW

✔ Java backend skeleton
✔ Enhanced Dijkstra (Java)
✔ Complaint & review API design
✔ Full frontend wireframe
✔ Strong academic justification

---

## 🚀 NEXT STEP (VERY IMPORTANT)

Tell me what you want **next in code form**:

* 🔧 **Complete ComplaintService Java code**
* 🧠 **Graph-building from DB for Dijkstra**
* 🌍 **Leaflet.js frontend starter code**
* 📝 **Viva Q&A (guaranteed questions)**

Just say the keyword — we’ll build it step by step 💙

